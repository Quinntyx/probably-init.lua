-- which-key: labels for the helix-style space menu. Most bindings carry their
-- own descriptions (set via vim.keymap.set), so only prefixes are declared here.

require("which-key").add({
    { "<space>w", group = "Window" },
})
